<script setup>
// Variables desde nuxt.config.ts (Referente al entorno, variables para despliegue)
const runtimeConfig = useRuntimeConfig()

// Variables desde app.config.ts (Referente a la aplicación, variables para configuración)
const appConfig = useAppConfig()
</script>
